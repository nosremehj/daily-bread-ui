# Referência: o app usa src/environments/environment.ts (dev) e environment.prod.ts (prod).
# Local e produção: http://localhost:9090 | https://api-daily-bread.onrender.com
#
# npm run start / start:local → API em localhost:9090
# npm run start:remote-api   → dev server local, API no Render (testar back em produção)
