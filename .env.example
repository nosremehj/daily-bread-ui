# Referência: o app usa src/environments/*.ts (Angular não lê .env no build).
# API local: http://localhost:9090
# API publicada: veja src/environments/deployed-api-url.ts
#
# npm run start / start:local     → API em localhost:9090
# npm run start:remote-api        → dev local, API em deployed-api-url.ts
